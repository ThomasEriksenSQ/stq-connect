

# Plan: Bytt fra horisontale til vertikale prikker

Endre `MoreHorizontal` til `MoreVertical` fra lucide-react i `ContactCardContent.tsx`.

## Endringer i `src/components/ContactCardContent.tsx`

1. **Import**: Bytt `MoreHorizontal` til `MoreVertical` i import-linjen (linje ~40)
2. **Bruk**: Bytt `<MoreHorizontal className="h-4 w-4" />` til `<MoreVertical className="h-4 w-4" />` (linje ~807)

Ingen andre filer endres.

